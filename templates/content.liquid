{% extends theme.liquid %}
{% block contents %}

<div class="container">
    <h1>{{ page.title }}</h1>
    {% editable_region about %}
        Default text
    {% endeditable_region %}
</div>
     
{% endblock %}